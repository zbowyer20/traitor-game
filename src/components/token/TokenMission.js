import React, {PropTypes} from 'react';
import ReactCSSTransitionGroup from 'react-addons-css-transition-group';

const TokenMission = () => {
  return (
    <div className="token_mission">
      <img src="../../images/mission-token.jpg" />
    </div>
  );
};

TokenMission.propTypes = {

};

export default TokenMission;
