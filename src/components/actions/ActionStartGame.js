import React, {PropTypes} from 'react';
const ActionStartGame = ({}) => {
  return (
    <div className="action">
      Start Game
    </div>
  );
};

ActionStartGame.propTypes = {
};

export default ActionStartGame;
